# dev-env
scripts to quickly setup a development environment with my preferred configurations

dev-env:  purpose is to copy dot files in .config folder to $XDG_CONFIG_HOME

run: purpose is to run all scripts in the runs folder which will install and set up any programs I want
